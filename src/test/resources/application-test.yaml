spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cashpilot_test
    username: cashpilot_test
    password: ${TEST_DB_PASSWORD}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: true

app:
  environment: test
  mail:
    from: no-reply@cashpilot.test
    verification-url: http://localhost:5173/confirm-email